import Typeography from 'typography';
import fairyGateTheme from 'typography-theme-fairy-gates';

const typography = new Typeography(fairyGateTheme);

export const {
  scale,
  rhythm,
  options,
} = typography;

export default typography;
